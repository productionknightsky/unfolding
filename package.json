{
  "name": "unfolding",
  "private": true,
  "version": "1.0.0",
  "description": "Unfolding - self-reflection social app (Node + React)",
  "scripts": {
    "install-all": "npm install --prefix server && npm install --prefix client",
    "dev": "concurrently \"npm start --prefix server\" \"npm run dev --prefix client\"",
    "start": "npm run dev"
  },
  "devDependencies": {
    "concurrently": "^9.0.0"
  }
}